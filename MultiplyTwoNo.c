// Multiply two no
#include <stdio.h>
int main() {
int a,b;
printf("Enter two nos to be multiplied:");
   scanf("%d %d", &a,&b);
   printf("The multiplication of %d and %d is %d.",a,b,a*b);
    return 0;
}
