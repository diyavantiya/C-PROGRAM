// Convert hours into mins 
#include <stdio.h>
int main() {
float hours;
printf("Enter the no of hours that need to be converted to mins:");
   scanf("%f", &hours);
    printf("%f hours = %f minutes ",hours,hours*60);
    return 0;
}
