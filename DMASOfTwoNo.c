// Add, Subtract, Multipy and Divide two no
#include <stdio.h>
int main() {
float a,b;
printf("Enter two nos to be added, subtracted, multiplied and divided:");
   scanf("%f %f", &a,&b);
    printf("The additon of %f and %f is %f\n",a,b,a+b);
    printf("The subtraction of %f and %f is %f\n",a,b,a-b);
    printf("The multiplication of %f and %f is %f\n",a,b,a*b);
    printf("The division of %f and %f is %f",a,b,a/b);
    return 0;
}
