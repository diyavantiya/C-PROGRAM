// Convert kg to grams 
#include <stdio.h>
int main() {
float kg;
printf("Enter the weight in kg to convert into grams:");
   scanf("%f", &kg);
    printf("%f kg = %f grams ",kg,kg*1000);
    return 0;
}
